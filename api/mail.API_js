{"POST":"var email = mail(); // Returns mail object \r\nvar subject = 'Pitanja, sugestije, predlozi'; // variable subject is string type\r\nemail.subject(subject); // Adds variable 'subject' to mail object\r\nvar from =param('senderEmail'); // from is string type, it represents address from where it is email sent\r\nemail.from(from); // adds from to mail object\r\nvar message = param('senderMessage');\r\n\r\nemail.message(message); // Adds message to mail object\r\nvar to = 'ivanugljesic@gmail.com'; // variable to is string type, it represents address where the email is sent\r\nemail.to(to); // Adds variable 'to' to mail object, if there is already some addresses overrides them!\r\nvar addTo = 'ivanugljesic@gmail.com';  // string type, it represents address to which it is email sent\r\nemail.addTo(addTo); // Adds new address to already existed addresses\r\nemail.send(); // Sends an email with these parameters"}